int main(void)
{
        int array[10];
        struct person parray[30];

        int, double, char, char *, struct person,
            ...

                /* what you can do with it */
                numeric types : support +
            , -, *,
            / ... struct supports.operator struct *supports->
                operator void *... don't know the operations of the type , type safety.  int
                add(int a, int b)
        {
        }

        for (count = 0; count < size; ++count)
        {
                doit(array[count]);
                doit(*(array + count));
        }
}
