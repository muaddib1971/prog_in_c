#include <stdio.h>
#include <stdlib.h>

int main(void)
{
        printf("%ld\n", sizeof(unsigned int));
        printf("%ld\n", sizeof(size_t));
        printf("%ld\n", sizeof(long));
        return EXIT_SUCCESS;
}
